# Backend origin the SPA talks to. Read at BUILD time (Vite inlines it), so a
# change needs a rebuild/redeploy.
#   local:      http://localhost:8787
#   production: set in the Vercel dashboard to https://<backend>.fly.dev
VITE_BACKEND_URL=http://localhost:8787
